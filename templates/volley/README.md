# templates/volley/README

Copy this folder's files into `volleys/YYYY-MM-DD/` each day, rename the date,
fill in the frontmatter. The schema lives in docs/FILE-SCHEMA.md. The daily
prompt lives in docs/DAILY-SKILL-PROMPT.md.
